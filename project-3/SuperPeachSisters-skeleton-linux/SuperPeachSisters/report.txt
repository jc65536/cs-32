Part 1. A high-level description of each of your public member functions in
        each of your classes, and why you chose to define each member function
        in its host class; also explain why (or why not) you decided to make
        each function virtual or pure virtual.

class StudentWorld

list<Actor *> findCollidingActors(Actor *self, double x = -1, double y = -1)
- Returns a list of actors that collide with self, with optional x an y
  parameters to test a hypothetical location.
- I declared this function in the StudentWorld class because StudentWorld has
  the most direct access to the list of actors.
- This function is not virtual since StudentWorld is not a base class.

Peach *getPeach()
- Returns the pointer to Peach.
- I declared this function in the StudentWorld class because it holds the
  single pointer to Peach.
- This function is not virtual since StudentWorld is not a base class.

void addActor(Actor *actor)
- Adds the given actor pointer to the universal list of actors
- I declared this function in the StudentWorld class because some actors need
  to spawn new actors through their StudentWorld reference.
- This function is not virtual since StudentWorld is not a base class.

void setStatus(int status)
- Tells the move() function to return some other status code. Used by the flag
  and Mario classes.
- I declared this function in the StudentWorld class because it affects the
  behavior of the move() function.
- This function is not virtual since StudentWorld is not a base class.

class Actor

virtual void doSomething()
- Does nothing.
- I declared this function in the Actor class because StudentWorld needs to be
  able to tell any actor to do something in the move() function.
- This function is virtual but not pure virtual since it makes sense that if a
  derived class does not override doSomething(), it will do nothing by default.

virtual void bonk(Actor *other)
- Does nothing.
- I declared this function in the Actor class because actors need to be able to
  bonk any colliding actors through an Actor pointer.
- This function is virtual but not pure virtual since it makes sense that if a
  derived class does not override bonk(), it will do nothing by default.

virtual void takeDamage()
- Does nothing.
- I declared this function in the Actor class because actors (e.g. projectiles)
  need to be able to damage any colliding actors through an Actor pointer.
- This function is virtual but not pure virtual since it makes sense that if a
  derived class does not override takeDamage(), it will do nothing by default.

bool isPeach();
- Returns whether this actor is Peach.
- I declared this function in the Actor class because actors (e.g. enemies and
  blocks) need to check whether the actor that bonked it is Peach.
- This function is neither virtual nor pure virtual because the base
  implementation works for all derived classes.

virtual bool passable()
- Returns whether this actor can be passed through.
- I declared this function in the Actor class because actors need to check
  whether any colliding actors are passable through an Actor pointer.
- This function is pure virtual because only concrete classes derived from
  Actor can have the property of being passable, and there is no obvious
  default for most actors.

virtual bool damageable()
- Returns whether this actor can be damaged.
- I declared this function in the Actor class because actors need to check
  whether any colliding actors are damageable through an Actor pointer.
- This function is pure virtual because only concrete classes derived from
  Actor can have the property of being damageable, and there is no obvious
  default for most actors.

bool isAlive()
- Returns whether this actor is alive.
- I declared this function in the Actor class because StudentWorld needs to
  check whether an actor is alive through an Actor pointer and because all
  actors have an alive state that could be checked.
- This function is not virtual because the base implementation works for all
  derived classes.

class Peach

void doSomething()
- Decrements any countdowns, bonks all colliding actors, attempts to move based
  on user input and jumping status, and spawns fireballs if need be.
- I declared this function in the Peach class because the spec specifies these
  behaviors for Peach.
- This function is not virtual because Peach is not a base class.

void bonk()
- Makes Peach take damage.
- I declared this function in the Peach class because enemies need to be able
  to bonk Peach.
- This function is not virtual because Peach is not a base class.

void takeDamage()
- Decreases hp and/or kills Peach only if Peach is not invincible
- I declared this function in the Peach class because Peach needs to react to
  damage.
- This function is not virtual because Peach is not a base class.

void addPower(int power)
- Updates Peach's internal powers state to have the indicated power.
- I declared this function in the Peach class so that powerups can modify
  Peach's powers state.
- This function is not virtual because Peach is not a base class.

bool hasPower(int power)
- Returns whether Peach has a power.
- I declared this function in the Peach class because enemies need to know if
  Peach has star power.
- This function is not virtual because Peach is not a base class.

bool passable()
- Returns true.
- I declared this function in the Peach class because Peach needs to override
  Actor's pure virtual function.
- This function is not virtual because Peach is not a base class.

bool damageable()
- Returns true.
- I declared this function in the Peach class because Peach needs to override
  Actor's pure virtual function.
- This function is not virtual because Peach is not a base class.

class Flag

void doSomething()
- If it is colliding with Peach, updates score and sets the game status to the
  appropriate status.
- I declared this function in the Flag class because the spec specifies these
  behaviors for Flag.
- This function is not virtual because Mario does not need to override it.

bool passable()
- Returns true.
- I declared this function in the Flag class because Flat needs to override
  Actor's pure virtual function.
- This function is not virtual because Mario does not need to override it.

bool damageable()
- Returns false.
- I declared this function in the Flag class because Flag needs to override
  Actor's pure virtual function.
- This function is not virtual because Mario does not need to override it.

class Powerup

void doSomething()
- If it is colliding with Peach, increase score and grant Peach the appropriate
  powers. Otherwise, move according to the spec.
- I declared this function in the Powerup class because the spec specifies
  these behaviors for all powerups.
- This function is not virtual because no powerup needs to override it.

bool passable()
- Returns true.
- I declared this function in the Powerup class because all powerups are
  passable.
- This function is not virtual because no powerup needs to override it.

bool damageable()
- Returns false.
- I declared this function in the Powerup class because no powerups are
  damageable.
- This function is not virtual because no powerup needs to override it.

class Enemy

void bonk(Actor *other)
- Makes the enemy take damage if other is Peach and she has star power.
- I declared this function in the Enemy class because all enemies handle bonks
  this way.
- This function is not virtual because no enemy needs to override it.

virtual void takeDamage()
- Increases the score and kills the enemy.
- I declared this function in the Enemy class because all enemies include this
  behavior when they take damage.
- This function is virtual but not pure virtual because Koopa does something
  extra when it takes damage, so it will need to override it. All other enemies
  do not need to override it.

double setMinX(double x)
- Sets the minX member variable.
- I declared this function in the Enemy class because StudentWorld needs to set
  an enemy's min x coordinate based on the previous row of actors.
- This function is not virtual because no enemy needs to override it.

double setMaxX(double x)
- Sets the maxX member variable.
- I declared this function in the Enemy class because StudentWorld needs to set
  an enemy's max x coordinate based on the previous row of actors.
- This function is not virtual because no enemy needs to override it.

bool passable()
- Returns true.
- I declared this function in the Enemy class because all enemies are passable.
- This function is not virtual because no enemy needs to override it.

bool damageable()
- Returns true.
- I declared this function in the Enemy class because all enemies are
  damageable.
- This function is not virtual because no enemy needs to override it.

class Goomba

void doSomething()
- Bonks peach if it's colliding with her and moves according to the spec.
- I declared this function in the Goomba class because the spec specifies these
  behaviors for Goomba.
- This function is not virtual because Koopa does not need to override it.

class Koopa

void takeDamage()
- Increases score, kills the Koopa, and spawns a new Shell.
- I declared this function in the Koopa class because it needs to do more than
  Enemy's takeDamage().
- This function is not virtual because Koopa is not a base class.

class Piranha

void doSomething()
- Cycles animation, bonks Peach if it's colliding with her, and shoots
  fireballs if the conditions are right.
- I declared this function in the Piranha class because the spec specifies these
  behaviors for Piranha.
- This function is not virtual because Piranha is not a base class.

class Pipe

bool passable()
- Returns false.
- I declared this function in the Pipe class because Pipes and Blocks are not
  passable.
- This function is not virtual because Block does not need to override it.

bool damageable()
- Returns false.
- I declared this function in the Pipe class because Pipes and Blocks are not
  damageable.
- This function is not virtual because Block does not need to override it.

class Block

void bonk(Actor *other)
- Spawns a powerup once if it was configured to do so; otherwise, play a sound.
- I declared this function in the Block class because the spec specifies these
  behaviors for Block.
- This function is not virtual because Block is not a base class.

class Projectile

void doSomething()
- If it's colliding with a suitable actor, bonk it and die; otherwise, move
  according to the spec.
- I declared this function in the Projectile class because all projectiles bonk
  actors and move the same way.
- This function is not virtual because no projectile needs to override it.

bool passable()
- Returns true.
- I declared this function in the Projectile class because all projectiles are
  passable.
- This function is not virtual because no projectile needs to override it.

bool damageable()
- Returns false.
- I declared this function in the Projectile class because no projectiles are
  damageable.
- This function is not virtual because no Projectile needs to override it.


Part 2. A list of all functionality that you failed to finish as well as known
        bugs in your classes.

I don't believe I am missing any functionality. A known bug is that no sound
would play, even though I think I'm using the playSound function correctly. In
addition to playing sound, I have added cerr statements to display any sound
events.


Part 3. A list of other design decisions and assumptions you made.

- I assumed that Peach can only bonk a single actor per tick. The sample
  program's behavior suggests that this is the right thing to do.

- I assumed that storing a pointer to a spawn function does not violate the
  scary red text on pages 28 and 49 because the FAQ claims that using a private
  enum is not a violation as long as there are no member functions to let
  outsiders know the type of object.
